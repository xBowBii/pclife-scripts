while (true) do {
if (alive S1) then {
        "S1Spawn" setmarkerpos (getpos S1);
};
sleep 0.25;
if (alive S2) then {
        "S2Spawn" setmarkerpos (getpos S2);
};
sleep 0.25;
if (alive S3) then {
        "S3Spawn" setmarkerpos (getpos S3);
};
sleep 0.25;
if (alive S4) then {
        "S4Spawn" setmarkerpos (getpos S4);
};
sleep 0.25;
if (alive S5) then {
        "S5Spawn" setmarkerpos (getpos S5);
};
sleep 0.25;
if (alive S6) then {
        "S6Spawn" setmarkerpos (getpos S6);
};
sleep 0.25;
if (alive S7) then {
        "S7Spawn" setmarkerpos (getpos S7);
};
sleep 0.25;
if (alive S8) then {
        "S8Spawn" setmarkerpos (getpos S8);
};
sleep 10;
};